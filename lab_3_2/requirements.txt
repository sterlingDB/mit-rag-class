# Pinned dependencies for the MO-LLM labs (Modules 1-4).
# Tested in a fresh Python 3.12 virtual environment.
#
# These versions are pinned together to avoid the package-drift problems
# (RAGAS/LangChain import errors, OpenRouter embedding failures) that appear
# with unpinned installs. Install with:
#     python -m venv .venv
#     #  Windows:  .venv\Scripts\activate     macOS/Linux:  source .venv/bin/activate
#     python -m pip install --upgrade pip
#     pip install -r requirements.txt

python-dotenv==1.2.2

# LangChain ecosystem (pinned as a compatible set)
langchain-core==1.3.2
langchain-openai==1.1.10
langchain-chroma==1.1.0
langchain-community==0.4.1
langchain-text-splitters==1.1.1

# Vector store (Modules 2-4)
chromadb==1.5.9

# OpenAI SDK (used directly by the Lab 3.1 RAGAS judge)
openai==2.26.0

# Evaluation — Lab 3.1 uses the RAGAS Experiments API, which needs 0.4.x (NOT 0.2.x)
ragas==0.4.3

# Keyword retrieval (Modules 1-4)
rank-bm25==0.2.2

# Knowledge graph (Lab 4.2)
networkx==3.6.1

# Common transitive dependencies that frequently drift — pinned for reproducibility
numpy==2.2.4
tiktoken==0.12.0
pydantic==2.12.5
tenacity==9.1.2
datasets==4.8.5
